 <!-- Breadcrumbs  -->
<div class="breadcrumbs">
	<div class="container">
		 
		 <ul class="bclist">
			<li><a href="{{site.url}}" rel="home">{{site.title}}</a>&nbsp;</li>
			<li>/&nbsp;<a href="{{link}}">{{title}}</a></li>			
		 </ul>

			<!-- titre  -->
		  {% if title %}
		  		<h1 class="title">{{title}}</h1>
		  {% else %}
				 <h1 class="title">{{post.title}}</h1>
		  {% endif %}
	</div>
</div>